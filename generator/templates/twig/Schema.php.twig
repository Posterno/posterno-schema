<?php

namespace PNO\SchemaOrg;

/**
 * Factory class for all Schema.org types.
 */
class Schema
{
{% for type in types %}    public static function {{ type.name | lcfirst }}()
    {
        return new {{ type.name }}();
    }

{% endfor %}
}
